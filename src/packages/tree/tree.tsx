import {defineComponent} from "@vue/composition-api";

export default defineComponent({
    name: 'pl-tree',
    props: {
        
    },
    setup(props) {
        return () => (
            <div>
                pl tree
            </div>
        )
    },
})