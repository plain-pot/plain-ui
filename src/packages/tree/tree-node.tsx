import {defineComponent} from "@vue/composition-api";

export default defineComponent({
    name: 'pl-tree-node',
    props: {},
    setup(props) {
        return () => (
            <div>
                pl tree
            </div>
        )
    },
})