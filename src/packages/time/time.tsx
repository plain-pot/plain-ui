import {defineComponent} from "@vue/composition-api";

export default defineComponent({
    name: 'pl-time',
    props: {
        
    },
    setup(props) {
        return () => (
            <div>
                
            </div>
        )
    },
})