import {defineComponent} from "@vue/composition-api";

export default defineComponent({
    name: 'pl-cascade',
    props: {
        
    },
    setup(props) {
        return () => (
            <div>
                cascade
            </div>
        )
    },
})