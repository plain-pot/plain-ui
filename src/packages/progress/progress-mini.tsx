import {defineComponent} from "@vue/composition-api";

export default defineComponent({
    name: 'pl-progress-mini',
    props: {

    },
    setup(props) {
        return () => (
            <div>

            </div>
        )
    },
})