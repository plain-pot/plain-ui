import {designComponent} from "../../use/designComponent";

export default designComponent({
    name: 'pl-form-item',
    setup() {
        return {
            render: () => (
                <div class="pl-form-item">

                </div>
            )
        }
    },
})