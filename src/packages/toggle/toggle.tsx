import {defineComponent} from "@vue/composition-api";

export default defineComponent({
    name: 'pl-toggle',
    props: {
        
    },
    setup(props, context) {
        return () => (
            <div>
                
            </div>
        )
    },
})