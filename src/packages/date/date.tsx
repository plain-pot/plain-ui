import {defineComponent} from "@vue/composition-api";

export default defineComponent({
    name: 'pl-date',
    props: {
        
    },
    setup(props) {
        return () => (
            <div>
                
            </div>
        )
    },
})