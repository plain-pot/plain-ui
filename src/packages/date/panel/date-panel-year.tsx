import {designComponent} from "../../../use/designComponent";

export const PlDatePanelYear = designComponent({
    props: {},
    setup({props}) {



        return {
            render: () => {

            }
        }
    },
})