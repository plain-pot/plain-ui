import {defineComponent} from "@vue/composition-api";

export default defineComponent({
    name: 'pl-dropdown',
    props: {
        
    },
    setup(props, context) {
        return () => (
            <div>
                
            </div>
        )
    },
})