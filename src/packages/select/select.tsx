import {defineComponent} from "@vue/composition-api";

export default defineComponent({
    name: 'pl-select',
    props: {
        
    },
    setup(props) {
        return () => (
            <div>
                pl-select
            </div>
        )
    },
})