import {defineComponent} from "@vue/composition-api";

export default defineComponent({
    name: 'pl-tab',
    props: {},
    setup(props) {
        return () => (
            <div>
                pl-tab
            </div>
        )
    },
})