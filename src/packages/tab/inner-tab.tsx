import {defineComponent} from "@vue/composition-api";

export default defineComponent({
    name: 'pl-inner-tab',
    props: {},
    setup(props) {
        return () => (
            <div>
                pl-inner-tab
            </div>
        )
    },
})