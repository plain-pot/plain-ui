import {defineComponent} from "@vue/composition-api";

export default defineComponent({
    name: 'pl-tab-group',
    props: {},
    setup(props) {
        return () => (
            <div>
                pl-tab-group
            </div>
        )
    },
})