export enum TabHeadStyle {
    default = 'default',
    card = 'card',
    integrate = 'integrate',
}

export enum TabPosition {
    top = 'top',
    bottom = 'bottom',
    left = 'left',
    right = 'right',
}