export enum TabHeadType {
    default = 'default',
    card = 'card',
    integrate = 'integrate',
}

export enum TabHeadPosition {
    top = 'top',
    bottom = 'bottom',
    left = 'left',
    right = 'right',
}