import {defineComponent} from "@vue/composition-api";

export default defineComponent({
    name: 'pl-virtual-tree',
    props: {
        
    },
    setup(props) {
        return () => (
            <div>
                virtual-tree
            </div>
        )
    },
})