import {defineComponent} from "@vue/composition-api";

export default defineComponent({
    name: 'pl-color-picker',
    props: {
        
    },
    setup(props, context) {
        return () => (
            <div>
                
            </div>
        )
    },
})