export default {
    name: 'column',
    data(){
        return{
            // normal(h, {row, editRow, showRow, text,rowIndex, col, colIndex, prop}) {return <span>column:{text}</span>},
        }
    },
}