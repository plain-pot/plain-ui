export default {
    name: 'column',
    props: {
        import: {default: false},
        export: {default: false},
        formEditable: {default: false},
    },
    data() {
        return {
            // normal(h, {row, editRow, showRow, text,rowIndex, col, colIndex, prop}) {return <span>column:{text}</span>},
        }
    },
}
